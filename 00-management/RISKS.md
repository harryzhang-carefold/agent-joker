# RISKS — agent-joker

| ID | 风险 | 等级 | 状态 | 应对 |
|---|---|---|---|---|
| RISK-001 | 05-temp/assets/ 原始素材目录不存在，需求以 BRIEF.md 中用户原话逐条为准 | 中 | CLOSED | 已接受：设计全部基于 BRIEF，不确定处标注「推测」，DESIGN_REVIEW 已汇总推测项清单并跟踪 |
| RISK-002 | 9 模块设计文档体量大，字段逐行要求严格，单 worker 可能超长 | 中 | CLOSED | 已消化：拆 4 卡（流程图/功能清单并行 + 架构&DB + 终审）+ 自验脚本（validate_d03.py），设计阶段全部交付并自验通过 |
| RISK-003 | BRIEF 两处表述存在歧义：①第三方 agent 的 RAG 检索是「平台侧执行并提供信息」还是「agent 调用 MCP 检索工具」；②简易 agent 是否也经平台 MCP 工具上传/查询文档 | 中 | OPEN | 已按「双通道并存 / 简易 agent 双路径」裁定（ARCH §9-6/9-7），开发前按双路径实现（两路径都可用、不互斥，风险最低）；**维持 OPEN，待罗辑/用户最终确认**（DESIGN_REVIEW §4.1 R2 / §6.5） |
| RISK-004 | 不同 embedding 模型维度不同，pgvector 向量列维度是表级固定的；DB_DESIGN 统一按 1536 维存储（<1536 补零、>1536 建库拒绝 422），补零对余弦相似度无影响属【推测】 | 中 | CLOSED | **已裁定（用户裁定 2026-09-22，D-C / DECISION-024）**：向量维度不固定、**按各知识库所选 embedding 模型维度存储，落地为每库独立向量表 `rag_chunks_vec_<kb_id>`**（命名 `rag_chunks_vec_<kb_id>`，建库时动态建表）；检索只走该 KB 自己的向量表；KB 换 embedding 模型 → 全量重算向量（新建按新维度表 → 全量重嵌入 → 切换 → DROP 旧表）；`rag_knowledge_bases.embedding_dim` 记该库实际维度（建库快照）；废原「统一 1536 + 补零 + 422 拒绝」。落点：DB_DESIGN §4.4（新增 rag_chunks_vec 表）/ §4.1（embedding_dim 建库快照、status 换模型重算）/ §4.3（rag_chunks.embedding 说明）/ §13（idx_chunks_vec_embedding 按维度 N + FK）/ §14/§15（表数 33→34）；ARCH §2.2（向量化/检索/建库流程）/ §7 R01 / §6#4 / §8（DECISION-024） |
| RISK-005 | trace_events + api_audit_logs 事件级明细表随会话量增长，存储/查询压力上升（90 天保留策略属【推测】） | 中 | CLOSED | **已裁定（用户裁定 2026-09-22，D-D / DECISION-025）**：保留天数**可配置**（环境变量 `TRACE_RETENTION_DAYS` / `AUDIT_RETENTION_DAYS`，**默认 90 天**，默认值为设计决定，去【推测】）；实现沿用既有设计——**按月分区 + 定期任务按配置天数 DROP PARTITION / 清理**（参数化，非硬编码）；主查询索引 tenant_id 打头；超大规模时归档到对象存储（迭代项）；开发阶段监控表体积。落点：DB_DESIGN §1.8（api_audit_logs 保留策略）/ §9.2（trace_events 保留策略）；ARCH §2.2 / 保留策略处；`.env.example` 增两个变量（默认 90） |
| RISK-006 | BFF「Agent 工具调用必须经统一拦截」边界 | 中 | CLOSED | **已裁定（用户裁定 2026-09-22，D-B / DECISION-023）**：拦截范围 = ①agent 对接业务系统统一经 BFF 网关；②MCP 工具调用（含平台内置 upload_doc/query_doc/rag_search 与 URL 注册的外部 MCP server）100% 经 BFF ToolInterceptor（scope 校验、Access Token 强制注入、机器凭证代理执行）。非拦截范围 = 简易 agent 对平台内部服务的直接 API 调用（RAG 检索/文件上传下载），不产生 tool_call 事件，但保留身份校验(tenant/scope) + (agent_id,kb_id) 勾选校验(未勾选403) + rag/file 事件留痕。ARCH §1.1 已为单一权威表述，§9-7 改「已裁定（D-B）」，BFF-06 验收 1 排除项成文。D08 复核 PASS（REVISION_VERIFY §3） |
| RISK-007 | BRIEF「39 条」计数口径与展开 45 个标准 ID 不符，削弱「逐条无遗漏」可验证性 | 低 | CLOSED | **已修复（2026-09-22 v2 修订）**：4 文档统一口径为「41 条 bullet（4 条含子项）→ 45 个标准 ID」（FEATURES §10.1 / ARCH §7）；连带勘误 DB §14「25→33 张表」笔误、文首「§7 九张」补注（1+4+4=9）。D08 复核确认 4 文档内「39 条」口径残留 = 0（REVISION_VERIFY §4） |
| OBS-01 | 限流 QPS 敏感（多登录/高并发下 refresh 轮换与 429 边界） | P3 | OPEN | 生产按实际 QPS 调 `RATE_LIMIT_*`；功能不受阻（S14/S15 已定位口径） |
| OBS-02 | 真实 LLM 端点 34.121.9.233:4000 key 401（端点侧 worker key 配置抖动，环境态非平台缺陷） | 低 | OPEN（已缓解） | S21/S23 持续 401 → S25 真实 key 探测 ok=true 已恢复；若再抖动需用户端点侧核查 worker `--api-key` 一致性；平台侧 401 结构化判定 + mock/本地 fallback 兜底，无需改代码 |
